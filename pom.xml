<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>uk.gov.justice</groupId>
        <artifactId>maven-parent-pom</artifactId>
        <version>25.104.1</version>
    </parent>

    <artifactId>maven-framework-parent-pom</artifactId>
    <version>25.104.1</version>
    <packaging>pom</packaging>
    <name>Framework parent POM</name>
    <description>Parent POM for the Microservices framework</description>

    <properties>
        <plugins.maven.wildfly.version>6.0.0.Final</plugins.maven.wildfly.version>
        <plugins.h2-maven.version>1.0</plugins.h2-maven.version>
        <plugins.exec-maven.version>3.0.0</plugins.exec-maven.version>
        <jandex.version>3.1.6</jandex.version>
        <cpp.repo.name>maven-framework-parent-pom</cpp.repo.name>
        <maven.common.bom.version>25.104.1</maven.common.bom.version>

        <!--
            jakarta.xml.bind-api is managed by the BOM at 4.0.0 (Jakarta EE 10,
            namespace jakarta.xml.bind.*). The RAML parser (org.raml:raml-parser) and
            coveralls-maven-plugin 4.3.0 use javax.xml.bind.* classes internally.
            Those classes only exist in versions 2.x of jakarta.xml.bind-api (before the
            EE 9 namespace change). From 3.0.0 onwards javax.xml.bind.* is no longer
            provided. Any plugin that uses the RAML parser or coveralls-maven-plugin must
            pin its runtime classpath to this version. DO NOT upgrade to 3.x or 4.x.
        -->
        <jakarta.xml.bind-api.raml.version>2.3.2</jakarta.xml.bind-api.raml.version>

        <!--
            snakeyaml version for the Liquibase fat JAR only. Liquibase 4.30.0 requires
            snakeyaml 2.x (e.g. TagInspector added in 2.0). The project classpath uses
            snakeyaml 1.33 (see cp-maven-parent-pom) to keep the RAML parser working.
            The fat JAR's snakeyaml is relocated (see shade config) so it cannot conflict
            with the Liquibase Maven plugin's own snakeyaml on the classloader.
        -->
        <snakeyaml.liquibase.version>2.3</snakeyaml.liquibase.version>
    </properties>

    <scm>
        <connection>${cpp.scm.connection}</connection>
        <developerConnection>${cpp.scm.developerConnection}</developerConnection>
        <url>${cpp.scm.url}</url>
    </scm>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>uk.gov.justice</groupId>
                <artifactId>maven-common-bom</artifactId>
                <version>${maven.common.bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!--
    =====
    BUILD
    =====
    -->

    <build>

        <!-- Default plugin versions and management -->
        <pluginManagement>
            <plugins>

                <!-- WildFly plugin to deploy war -->
                <plugin>
                    <groupId>org.wildfly.plugins</groupId>
                    <artifactId>wildfly-maven-plugin</artifactId>
                    <version>${plugins.maven.wildfly.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.liquibase</groupId>
                    <artifactId>liquibase-maven-plugin</artifactId>
                    <version>${liquibase.version}</version>
                    <configuration>
                        <skip>${skipTests}</skip>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>com.edugility</groupId>
                    <artifactId>h2-maven-plugin</artifactId>
                    <version>${plugins.h2-maven.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>com.h2database</groupId>
                            <artifactId>h2</artifactId>
                            <version>${h2.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>
                <plugin>
                    <groupId>org.bsc.maven</groupId>
                    <artifactId>maven-processor-plugin</artifactId>
                    <version>${plugins.maven.processor.version}</version>
                    <executions>
                        <execution>
                            <id>process</id>
                            <goals>
                                <goal>process</goal>
                            </goals>
                            <phase>generate-sources</phase>
                            <configuration>
                                <processors>
                                    <processor>
                                        org.hibernate.jpamodelgen.JPAMetaModelEntityProcessor
                                    </processor>
                                </processors>
                            </configuration>
                        </execution>
                    </executions>
                    <dependencies>
                        <dependency>
                            <groupId>org.hibernate</groupId>
                            <artifactId>hibernate-jpamodelgen</artifactId>
                            <version>${hibernate.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>

                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${plugins.exec-maven.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${plugins.maven.resources.version}</version>
                    <executions>
                        <execution>
                            <id>copy-json-test-resources</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>copy-resources</goal>
                            </goals>
                            <configuration>
                                <outputDirectory>${basedir}/target/generated-test-resources/json
                                </outputDirectory>
                                <resources>
                                    <resource>
                                        <directory>src/raml/json</directory>
                                        <filtering>true</filtering>
                                    </resource>
                                </resources>
                            </configuration>
                        </execution>
                        <execution>
                            <id>copy-json-schema-resources</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>copy-resources</goal>
                            </goals>
                            <configuration>
                                <outputDirectory>${basedir}/target/generated-resources/json/schema
                                </outputDirectory>
                                <resources>
                                    <resource>
                                        <directory>src/raml/json/schema</directory>
                                        <filtering>true</filtering>
                                    </resource>
                                </resources>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

            </plugins>

        </pluginManagement>

        <!-- Plugin configuration -->
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${plugins.jacoco.version}</version>
                <configuration combine.children="append">
                    <excludes>
                        <exclude>uk/gov/justice/api/*Application.class</exclude>
                        <exclude>uk/gov/justice/api/*JmsListener.class</exclude>
                        <exclude>uk/gov/justice/api/Remote*.class</exclude>
                        <exclude>uk/gov/justice/api/controller/Remote*.class</exclude>
                        <exclude>uk/gov/justice/api/resource/*Resource.class</exclude>
                        <exclude>uk/gov/justice/api/mapper/*ActionMapper.class</exclude>
                        <exclude>uk/gov/justice/api/mapper/*MediaTypeToSchemaIdMapper.class</exclude>
                        <exclude>entity/*_.class</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>

    </build>


    <!--
    =======================
    DISTRIBUTION MANAGEMENT
    =======================
    -->

    <!-- NOTHING SET YET -->

    <!--
    ========
    PROFILES
    ========
    -->

    <profiles>

        <profile>
            <id>raml-jar</id>
            <activation>
                <file>
                    <exists>src/raml</exists>
                </file>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-jar-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>raml</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                                <phase>package</phase>
                                <configuration>
                                    <classifier>raml</classifier>
                                    <classesDirectory>${project.basedir}/src</classesDirectory>
                                    <includes>
                                        <include>raml/**</include>
                                    </includes>
                                </configuration>
                            </execution>
                            <execution>
                                <id>raml-test</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                                <phase>generate-test-resources</phase>
                                <configuration>
                                    <classifier>raml</classifier>
                                    <classesDirectory>${project.basedir}/src</classesDirectory>
                                    <includes>
                                        <include>raml/**</include>
                                    </includes>
                                    <outputDirectory>${project.build.directory}</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>liquibase-jar</id>
            <activation>
                <file>
                    <exists>src/main/resources/liquibase.properties</exists>
                </file>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.liquibase</groupId>
                        <artifactId>liquibase-maven-plugin</artifactId>
                        <configuration>
                            <propertyFile>src/main/resources/liquibase.properties</propertyFile>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <configuration>
                            <archive>
                                <manifest>
                                    <addClasspath>true</addClasspath>
                                    <mainClass>liquibase.integration.commandline.LiquibaseCommandLine</mainClass>
                                </manifest>
                            </archive>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <configuration>
                            <dependencyReducedPomLocation>
                                ${project.build.directory}/dependency-reduced-pom.xml
                            </dependencyReducedPomLocation>
                            <artifactSet>
                                <excludes>
                                    <!-- Not needed: we only use open-source Liquibase operations. -->
                                    <exclude>org.liquibase:liquibase-commercial</exclude>
                                </excludes>
                            </artifactSet>
                            <relocations>
                                <!--
                                    Relocate snakeyaml into a private package so that when this fat
                                    JAR is added to the liquibase-maven-plugin classpath, the bundled
                                    snakeyaml cannot shadow the plugin's own snakeyaml 2.x. Without
                                    relocation, snakeyaml 1.x from the fat JAR hides TagInspector
                                    (snakeyaml 2.x) that liquibase-commercial:4.30.0 requires,
                                    causing ServiceConfigurationError on ChecksCreateCommandStep.
                                -->
                                <relocation>
                                    <pattern>org.yaml.snakeyaml</pattern>
                                    <shadedPattern>uk.gov.justice.shaded.snakeyaml</shadedPattern>
                                </relocation>
                            </relocations>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>

            <dependencies>
                <dependency>
                    <groupId>org.liquibase</groupId>
                    <artifactId>liquibase-core</artifactId>
                </dependency>
                <dependency>
                    <groupId>org.postgresql</groupId>
                    <artifactId>postgresql</artifactId>
                </dependency>
                <dependency>
                    <groupId>org.yaml</groupId>
                    <artifactId>snakeyaml</artifactId>
                    <version>${snakeyaml.liquibase.version}</version>
                </dependency>
                <dependency>
                    <groupId>info.picocli</groupId>
                    <artifactId>picocli</artifactId>
                </dependency>
            </dependencies>
        </profile>

        <profile>
            <id>jandex-index</id>
            <activation>
                <file>
                    <exists>src/main/java</exists>
                </file>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <dependencies>
                            <dependency>
                                <groupId>io.smallrye</groupId>
                                <artifactId>jandex</artifactId>
                                <version>${jandex.version}</version>
                            </dependency>
                        </dependencies>
                        <executions>
                            <execution>
                                <id>generate-jandex-index</id>
                                <phase>process-classes</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>java</executable>
                                    <arguments>
                                        <argument>-jar</argument>
                                        <argument>${settings.localRepository}/io/smallrye/jandex/${jandex.version}/jandex-${jandex.version}.jar</argument>
                                        <argument>-m</argument>
                                        <argument>${project.build.outputDirectory}</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

    </profiles>

</project>
